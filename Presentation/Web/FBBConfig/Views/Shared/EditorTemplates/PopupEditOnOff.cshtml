@model WBBEntity.PanelModels.FBBWebConfigModels.ConfigurationOnOffServices
@using Kendo.Mvc.UI
    @(Html.Kendo().DropDownList()
                .Name("Status")
                .DataTextField("Text")
                .DataValueField("Value")
                .HtmlAttributes(new { style = "width: 100%" })
                .BindTo(new List<SelectListItem>()
                {
                    new SelectListItem() {
                    Text = "ON", Value ="ON"
                    },
                    new SelectListItem() {
                    Text = "OFF", Value ="OFF"
                    }
                })
                .Events(e=>e.Change("onStatusChange"))
                    )
<script>

 
</script>